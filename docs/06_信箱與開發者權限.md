# 深層礦脈｜信箱與開發者權限 v0.10.1

> 這份解決兩件事：
> 1. 朋友有帳號之後，**開發者模式不能給他們用**
> 2. 你要能**發信給全體玩家、附獎勵**

---

## 1. 開發者模式怎麼鎖的

玩家版**根本不會下載 `editor.js`**。要叫出編輯模式，兩個條件同時成立才行：

1. 網址後面要帶 `?dev=1`
2. 而且 —— **在你自己電腦上直接開檔案**（`file://` 或 `localhost`），**或** 你的雲端帳號在 `admins` 名單裡

所以：

| 情況 | 結果 |
|---|---|
| 你在家用 Chrome 開 `index.html?dev=1` | ✎ 出現（本機一律放行，方便你開發） |
| 你在手機上 `https://...github.io/?dev=1`，已用你的帳號登入 | ✎ 出現 |
| 朋友在手機上打 `?dev=1`（沒在 admins 名單） | ✎ **不出現**，editor.js 也不會被下載 |
| 誰都沒加 `?dev=1` | ✎ 不出現 |

**老實說**：`editor.js` 這個檔案還是放在 GitHub 上，真的懂的人能自己叫起來。這擋的是「朋友手滑按到把數值改爛」，不是擋駭客。

---

## 2. 建資料表（貼一次就好）

Supabase → **SQL Editor** → **New query** → 整段貼上 → **Run**。

```sql
-- === 管理員名單 ===
create table if not exists public.admins (
  user_id uuid primary key references auth.users(id) on delete cascade,
  note    text
);
alter table public.admins enable row level security;
drop policy if exists "see self admin" on public.admins;
create policy "see self admin" on public.admins for select using (auth.uid() = user_id);

-- === 信件 ===
create table if not exists public.mail (
  id         bigint generated always as identity primary key,
  title      text not null,
  body       text not null default '',
  coins      bigint not null default 0,
  ore_name   text,
  ore_qty    int not null default 0,
  tool_id    text,
  tool_qty   int not null default 0,
  to_user    uuid references auth.users(id) on delete cascade,  -- null = 全體
  created_at timestamptz default now(),
  expires_at timestamptz
);
alter table public.mail enable row level security;
drop policy if exists "read my mail"  on public.mail;
drop policy if exists "admin writes"  on public.mail;
create policy "read my mail" on public.mail for select
  using (to_user is null or to_user = auth.uid());
create policy "admin writes" on public.mail for all
  using (exists (select 1 from public.admins a where a.user_id = auth.uid()))
  with check (exists (select 1 from public.admins a where a.user_id = auth.uid()));

-- === 領取記錄（主鍵擋住重複領） ===
create table if not exists public.mail_claims (
  user_id    uuid   references auth.users(id) on delete cascade,
  mail_id    bigint references public.mail(id) on delete cascade,
  claimed_at timestamptz default now(),
  primary key (user_id, mail_id)
);
alter table public.mail_claims enable row level security;
drop policy if exists "my claims read"   on public.mail_claims;
drop policy if exists "my claims insert" on public.mail_claims;
create policy "my claims read"   on public.mail_claims for select using (auth.uid() = user_id);
create policy "my claims insert" on public.mail_claims for insert with check (auth.uid() = user_id);
```

### 把自己設成管理員

**先用遊戲註冊你自己的帳號**，然後跑這一行（把 Email 換成你註冊用的）：

```sql
insert into public.admins (user_id, note)
select id, '我' from auth.users where email = 'notzutote0148@gmail.com'
on conflict do nothing;
```

---

## 3. 發信指令（這段是重點）

再貼一次下面整段，它會建立四個**一行就能用**的指令。建好之後你**永遠不用再寫 SQL**。

```sql
-- 發全體信
create or replace function public.send_mail(
  title text, body text default '', coins bigint default 0,
  ore text default null, ore_qty int default 0,
  tool text default null, tool_qty int default 0,
  days int default 30
) returns bigint language plpgsql security invoker as $$
declare new_id bigint;
begin
  -- 在 SQL Editor 執行時身分是 postgres、auth.uid() 為空，所以一併放行
  if not (exists (select 1 from public.admins where user_id = auth.uid())
          or current_user in ('postgres','supabase_admin')) then
    raise exception '你不是管理員，不能發信';
  end if;
  if coalesce(trim(title), '') = '' then raise exception '標題不能空白'; end if;
  if coins < 0 or coins > 1000000 then raise exception '金幣要在 0 ~ 1,000,000 之間（你填了 %）', coins; end if;
  insert into public.mail (title, body, coins, ore_name, ore_qty, tool_id, tool_qty, to_user, expires_at)
  values (title, body, coins, ore, ore_qty, tool, tool_qty, null, now() + (days || ' days')::interval)
  returning id into new_id;
  return new_id;
end $$;

-- 發給指定一個人（用他註冊的 Email）
create or replace function public.send_to(
  email text, title text, body text default '', coins bigint default 0,
  ore text default null, ore_qty int default 0,
  tool text default null, tool_qty int default 0,
  days int default 30
) returns bigint language plpgsql security definer set search_path = public as $$
declare uid uuid; new_id bigint;
begin
  -- 在 SQL Editor 執行時身分是 postgres、auth.uid() 為空，所以一併放行
  if not (exists (select 1 from public.admins where user_id = auth.uid())
          or current_user in ('postgres','supabase_admin')) then
    raise exception '你不是管理員，不能發信';
  end if;
  select id into uid from auth.users u where u.email = send_to.email;
  if uid is null then raise exception '找不到這個 Email 的玩家：%', email; end if;
  if coins < 0 or coins > 1000000 then raise exception '金幣要在 0 ~ 1,000,000 之間'; end if;
  insert into public.mail (title, body, coins, ore_name, ore_qty, tool_id, tool_qty, to_user, expires_at)
  values (title, body, coins, ore, ore_qty, tool, tool_qty, uid, now() + (days || ' days')::interval)
  returning id into new_id;
  return new_id;
end $$;

-- 收回一封信（發錯了用這個）
create or replace function public.unsend_mail(mail_id bigint)
returns text language plpgsql security definer set search_path = public as $$
declare n int;
begin
  if not (exists (select 1 from public.admins where user_id = auth.uid())
          or current_user in ('postgres','supabase_admin')) then
    raise exception '你不是管理員';
  end if;
  select count(*) into n from public.mail_claims c where c.mail_id = unsend_mail.mail_id;
  delete from public.mail m where m.id = unsend_mail.mail_id;
  return format('已收回第 %s 號信，已經有 %s 個人領過了（領過的不會被收回去）', mail_id, n);
end $$;

-- 看發過哪些信、幾個人領了
create or replace function public.list_mail()
returns table (id bigint, title text, reward text, target text, claimed bigint, sent_at timestamptz)
language sql security invoker as $$
  select m.id, m.title,
         concat_ws(' ', nullif('$' || m.coins, '$0'),
                        nullif(m.ore_name || ' x' || m.ore_qty, null),
                        nullif(m.tool_id || ' x' || m.tool_qty, null)),
         case when m.to_user is null then '全體' else '指定個人' end,
         (select count(*) from public.mail_claims c where c.mail_id = m.id),
         m.created_at
  from public.mail m order by m.id desc limit 50;
$$;
```

### 怎麼用（複製改字就好）

到 **SQL Editor**，貼一行，按 Run：

```sql
-- 純公告，不給獎勵
select send_mail('版本更新 v0.9.7', '新增了信箱，之後有活動會從這裡發獎勵。');

-- 給全體 5000 金幣
select send_mail('開幕慶', '感謝大家幫忙試玩！', 5000);

-- 給全體 5000 金幣 + 一把鐵鎬
select send_mail('開幕慶', '感謝大家幫忙試玩！', 5000, null, 0, 'iron', 1);

-- 單獨補償某個人
select send_to('朋友的@email.com', '存檔補償', '上次卡住很抱歉。', 20000);

-- 看發過什麼
select * from list_mail();

-- 發錯了，收回第 3 號信
select unsend_mail(3);
```

### 為什麼搞砸不了

- 這些指令**只碰 `mail` 這張表**，碰不到玩家存檔。最慘的狀況就是多發一封信，`unsend_mail` 收回。
- 打錯字會直接跳中文錯誤（「標題不能空白」「找不到這個 Email 的玩家」），**不會半做半不做**。
- 金幣超過 100 萬會被擋下來，手滑多打兩個 0 不會出事。
- 不是管理員執行 → 直接被拒絕。

### 先拿自己試

第一封信建議先發給自己確認流程：

```sql
select send_to('notzutote0148@gmail.com', '測試', '這是測試信', 100);
```

進遊戲按右上角 ✉ 看得到、領得到，再發全體的。

---

## 4. 遊戲裡長什麼樣

- 右上角多一個 **✉**，有沒領的信會冒紅點和數字
- 點開看到信件列表：標題、內文、附件獎勵、領取按鈕
- 領過的顯示「已領取」，過期的顯示「已過期」
- 沒登入雲端 → 信箱是空的，並提示要先登入

**領取流程**：先去雲端搶下「領取記錄」，搶到了才在本機加獎勵，然後存檔上傳。
所以同一封信**重複點、兩支手機同時點，都只會領到一次**（第二次會跳「這封信已經領過了」）。

---

## 5. 一樣要講清楚的限制

獎勵是**玩家的瀏覽器自己加上去的**，伺服器只記「誰領過」。
所以有人改前端，還是能給自己加金幣——這跟信箱無關，是整個遊戲在客戶端跑的必然結果。
真正要防，得把遊戲邏輯搬到伺服器，那是第三台之後的事。


## 6. 實際建置紀錄（2026-09-23）— 狀態 VERIFIED

三張表、四個指令都已經在真的資料庫上跑過，不再是「只有 SQL 寫好」。

| 項目 | 結果 |
|---|---|
| `admins` / `mail` / `mail_claims` 三張表 + RLS | Success |
| `admins` 有 1 筆（擁有者） | 確認 |
| `send_mail` / `send_to` / `unsend_mail` / `list_mail` | 建立完成 |
| 第一封測試信 `send_to(...)` | 回傳 mail_id = **1** |

### 過程中發現並修正的錯誤

**第一版的管理員檢查在 SQL Editor 裡會把自己擋掉。**

原本寫的是：

    if not exists (select 1 from public.admins where user_id = auth.uid()) then

在 SQL Editor 執行時，身分是資料庫管理員 `postgres`，**`auth.uid()` 是 NULL**，所以這個條件永遠成立 → 一律噴「你不是管理員，不能發信」。

改成同時放行資料庫管理員：

    if not (exists (select 1 from public.admins where user_id = auth.uid())
            or current_user in ('postgres','supabase_admin')) then

這是安全的：玩家透過網頁連進來時，`current_user` 只會是 `anon` 或 `authenticated`，**不可能是 `postgres`**，所以沒有被繞過的空間。

這個錯誤只有實際執行才會出現——語法完全正確，pglast 也驗得過。

---

## v0.10.8：玩家 ID、遊戲內管理信箱，以及上面那段修法的**嚴重錯誤**

### ⚠️ 先講錯誤：上一節的 `current_user` 修法是錯的，而且是個真的漏洞

上一節寫著「玩家透過網頁連進來時 `current_user` 只會是 `anon` 或 `authenticated`，不可能是 `postgres`」。

**這句話在 `security definer` 函式裡是錯的。**

`security definer` 的意思就是「執行時切換成函式擁有者的身分」。`send_to` 和 `unsend_mail` 都是 definer、擁有者是 `postgres`，所以函式**內部**的 `current_user` 永遠等於 `postgres`——那個 `or` 條件對任何呼叫者都成立，管理員檢查等於沒有。

後果：任何一個登入的玩家，在瀏覽器主控台打一行 RPC 就能寄一封帶 $1,000,000 的信給自己，或刪掉你發的信。（`send_mail` 是 `security invoker`，不受影響。）

這不是推論，是在本機 PostgreSQL 16 上實際跑出來的：以 `authenticated` 角色、非管理員身分呼叫舊寫法的函式，回傳「通過了管理員檢查」。

**正確寫法**是用 `session_user`（它在 definer 底下仍然是真正的連線身分），再加一道 `auth.uid() is null`：

    create or replace function public.is_admin_caller()
    returns boolean language sql stable security definer set search_path = public as $$
      select exists (select 1 from public.admins a where a.user_id = auth.uid())
          or (auth.uid() is null and session_user in ('postgres', 'supabase_admin'));
    $$;

`docs/07_管理員信箱UI升級.sql` 會把 `send_to`、`unsend_mail`、`send_mail` 一起換成呼叫這個函式，**跑那一份就順手補起來了**。

### 要做的事：執行一次 SQL → ✅ **2026-09-25 已執行完成**

Supabase → **SQL Editor** → **New query** → 把 `docs/07_管理員信箱UI升級.sql` **整份**貼上 → **Run**。
可以重複執行，跑第二次不會壞。跑完會看到 `Success`。

**2026-09-25 由擁有者授權，Claude 代為在正式專案執行，回傳 `Success. No rows returned`。**
執行後在正式資料庫查核的結果：

| 檢查 | 結果 |
|---|---|
| 帳號數 vs 已配發玩家 ID | 3 / 3（全部回填完成） |
| 玩家 ID 格式正確、不重複 | 3 / 3 |
| 十個新函式全部建立 | 10 / 10 |
| `send_to`／`unsend_mail`／`send_mail` 已改用 `is_admin_caller()` | 3 / 3 → **BUG-0 漏洞已封住** |

三位玩家都拿到 `auto` 來源的六碼 ID。

這份 SQL 會建立：

| 東西 | 做什麼 |
|---|---|
| `player_profiles` 表 | 每個帳號一組六碼玩家 ID（`100000`～`999999`），UNIQUE + 格式 CHECK |
| `my_player_id()` | 玩家拿自己的 ID，沒有就當場配一個 |
| `admin_change_player_id(舊, 新, 備註)` | 管理員改號，單一不可分割的操作 |
| `admin_send_self / _to_player / _all` | 三種寄信方式，輸入限制在資料庫端再驗一次 |
| `admin_list_mail()` | 最近 50 封，收件人只顯示六碼 ID |
| `admin_unsend_mail(id)` | 收回信件（不碰 `mail_claims`、不碰玩家存檔） |
| 回填 | 既有玩家自動補上玩家 ID |

### 玩家 ID 怎麼用

玩家在**地圖**頁面最下面的雲端存檔區塊會看到自己的六碼 ID 和「複製」按鈕。
朋友要補發獎勵時，把那組 ID 給你就好——**不要用玩家名稱**，名稱不唯一。

想給朋友一個特別的號碼：等他註冊完拿到隨機 ID，再到遊戲裡
**信箱 → 管理信箱 → 玩家 ID 管理**，填「目前的 ID」和「要改成的 ID」。
舊號碼會立刻釋出，之後可以再配給別人；新號碼如果已經有人用，整筆失敗、朋友的原號不變。

### 管理信箱怎麼開

登入管理員帳號 → 右上角 ✉ → 多一顆「**管理信箱**」按鈕（一般玩家看不到，也不需要 `?dev=1`）。

三個分頁：

- **寄信**：先寄給自己／指定玩家 ID／全體玩家。附件可以放金幣、礦石、工具。送出前會顯示玩家實際會看到的預覽，全體信要按兩次確認。成功後顯示 mail ID。
- **玩家 ID 管理**：改號。
- **寄件紀錄**：最近 50 封，可以收回。收回不會把已經領走的獎勵拿回來。

**前端藏按鈕不是安全機制。** 每一個動作都是 Supabase RPC，資料庫端會再驗一次身分；
一般玩家在主控台硬呼叫 `admin_send_all` 也會被擋掉（實測確認）。

### 這次的測試結果（本機 PostgreSQL 16，不是 Supabase）

| 測試 | 結果 |
|---|---|
| 三個帳號回填玩家 ID、格式正確、不重複 | 通過 |
| `my_player_id()` 多次呼叫回同一個號 | 通過 |
| 五碼／前導 0／七碼／英文 被 CHECK 擋掉 | 4/4 通過 |
| 非管理員改號 / 寄信 / 列表 / 收回 | 全部被拒 |
| 管理員改號成功、`id_source` 變 `admin` | 通過 |
| 新號已被占用 → 整筆失敗、原號不變 | 通過 |
| 舊號釋出後可以再配給別人 | 通過 |
| **兩個管理員同時搶同一個新號** | 一個成功、一個被 UNIQUE 擋下並乾淨回滾 |
| 寄信的所有邊界（標題 61 字、內文 1001 字、金幣超限、天數 0/366、礦石 1000、工具 100、有數量沒選品項） | 全部被擋 |
| 玩家只看得到自己的 `player_profiles`（RLS） | 3 列中只看得到 1 列 |
| 玩家只收得到「全體信 + 給自己的信」 | 通過 |
| 玩家自己 insert 一封信給自己 | 被擋 |

⚠️ **這是本機 PostgreSQL，不是 Supabase。** `auth.uid()` 是我用 session 變數模擬的，
PostgREST 的角色切換、Supabase 預設授權可能有細微差異。
**你在 Supabase 跑完之後還是要實際操作一次**，確認玩家 ID 有出現、管理信箱寄得出去。


---

## v0.10.10：彩色 ID（特別帳號的標記）

### 要做的事：再執行一次 SQL

Supabase → **SQL Editor** → **New query** → 把 `docs/08_玩家ID樣式.sql` 整份貼上 → **Run**。
可以重複執行。**必須先跑過 `docs/07`。**

這份 SQL 會：

| 東西 | 做什麼 |
|---|---|
| `player_profiles.id_style` 欄位 | `normal`（白色）或 `rainbow`（彩虹），有 CHECK 限制 |
| `my_player_profile()` | 玩家拿自己的號碼 + 樣式 |
| `admin_get_player(ID)` | 管理員查一個玩家目前的樣式、號碼來源、備註 |
| `admin_set_id_style(ID, 樣式)` | 管理員切換彩虹／一般 |
| 一行 update | 把 `admins` 名單裡的帳號預設設成彩虹（**只動還是預設值的那些**，你之後手動關掉不會被重新打開） |

### 怎麼用

**信箱 → 管理信箱 → 玩家 ID 管理**，最下面多一區「彩色 ID（特別帳號）」：
填六碼 ID → 「查詢」看目前狀態 →「設為彩虹」或「設回一般」。
改到自己的號碼時，地圖頁的 ID 會立刻跟著變。

彩色和改號是**兩件獨立的事**：可以給朋友一個特別號碼但不給彩色，也可以讓隨機號碼的人變彩色。

### 測試結果（本機 PostgreSQL 16）

| 測試 | 結果 |
|---|---|
| 玩家讀自己的號碼與樣式 | 通過 |
| 非管理員呼叫 `admin_set_id_style` / `admin_get_player` | 兩個都被拒 |
| 管理員設彩虹 → 玩家端讀到 rainbow | 通過 |
| 管理員設回一般 | 通過 |
| 樣式值填 `gold`（非法） | 被擋 |
| 五碼 ID／不存在的 ID | 被擋，訊息含輸入的號碼 |
| **改號之後樣式跟著人走**（777777→333333 仍是彩虹） | 通過 |
| 重複執行整份 SQL | 第二次 `UPDATE 0`，不會覆蓋手動設定 |

⚠️ 一樣是本機 PostgreSQL，不是 Supabase。跑完請實際在遊戲裡看一次。
