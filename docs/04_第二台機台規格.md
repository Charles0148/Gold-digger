# 深層礦脈｜第二台機台：三位前輩的考驗 v0.10.0

> 參考機台：《ToLOVEるダークネス（出包王女）》。
> 這座礦坑用的是**獨立的引擎**（`js/engine2.js`），跟前五座礦坑完全不共用抽選邏輯。

---

## 1. 定位

| 項目 | 內容 |
|---|---|
| 礦坑名稱 | 三位前輩的考驗（地圖上標示「玩法不同」） |
| 解鎖 | $5,000 |
| 建議工具 | 鐵鎬（第 3 階） |
| 天井 | 999 揮（累積最多機會牌的前輩會直接找你） |
| 機械割 | 設定1 約 125%、設定6 約 174%（v0.10.0 實測；數值以 config 為準） |
| 初當 | 約 1/284（設定1）～1/236（設定6）（抽選轉，不含演出轉） |

## 2. 三位前輩

| 前輩 | 個性 | 紫機會牌 | 出現率 | 金機會牌 | 約會通過率（設定1→6） | ST 對應牌通過率 |
|---|---|---|---|---|---|---|
| 岩倉（好過） | 女生，容易被逗笑。**認真想教你東西**就是升溫 | 岩倉的礦燈 | 1/102（設定共通） | 岩倉的金牌 1/641 | 45% → 49% | 90% → 92% |
| 赤井（中間） | 年輕男生，講話口語。**把你當兄弟／哥們**就是升溫 | 赤井的鑿子 | 1/169 → 1/135 | 赤井的金鑿 1/1077 → 1/862 | 55% → 59% | 80% → 84% |
| 霧島（最難） | 不苟言笑。**他笑出來**就是確信度最高 | 霧島的懷錶 | 1/308 → 1/205 | 霧島的金錶 1/1923 → 1/1282 | 70% → 74% | 70% → 76% |

台詞分三層：`chat`（一般）、`up`（升溫，綠色時用）、`hot`（確信，紅色時用），每層可放多句隨機挑。

- 三種機會牌是**獨立礦石**，跟前五座礦坑的紫礦／金礦沒有任何關係。
- 三位前輩的累積數與好感度**各自獨立**，而且**玩家完全看不到**。

## 3. 流程

```
通常：每揮抽小役（天井 999 揮；談話一開始就把天井計數歸零）
  紫機會牌 → 該前輩好感度 +1~10%（抽選）
  金機會牌 → 對應的前輩們好感度 +5~30%（抽選，保底 5%）
      甲的金 → 甲＋丙；乙的金 → 乙＋丙；丙的金 → 甲＋乙＋丙
  加完之後，立刻用「好感度」當機率抽「前輩找你談話」
      例：+5% → 抽 5% 沒中 → 之後 +7% → 抽 12% → ……一直到 100% 必定被找
  好感度 100% → 必定被找，而且該次約會必定過關
  好感度永久保留；離開礦坑 → 全部歸零
  若同一轉有兩位以上當選 → 先當選的先約會；失敗立刻接下一位（「○○也想找你聊聊」）
      成功時只有成功那位歸零，另一位的好感度保留

談話（約會）：長度 5～15 轉的對話演出（不吃耐久、不給礦石）
  成功率 = 該前輩自己的通過率（好感度只影響「被不被找」，不影響這裡）
  長度 1～2 轉 或 16～20 轉 = 違和感 → 確定過關（只在會過時才會出現，約 12%）
  每一轉抽一個「信賴度顏色」：白／藍／黃 → 一般台詞、綠 → 升溫台詞、紅 → 確信台詞
  紅色越多越容易過關（實測 0 次紅 9%、1 次 39%、2 次 86%、3 次以上 97% 以上）
  失敗 → 好感度保留（繼續累積）
  成功 → 該前輩好感度歸零 → 進入報酬，並把這一輪的所有狀態（關數、上位、對手）重算

報酬（AT）：10 轉，只賺錢（挖到的礦石只能賣）

ST 挑戰（最多一直連下去）：
  三位前輩走出來，隨機一位出現（上位時由玩家選）
  10 轉內抽到「對應的機會牌」→ 依前輩通過率抽選（90/80/70%）
  抽到別人的機會牌 → 25%（設定6 30%）
  抽到**任何一張金機會牌 → 直接過關（確定）**，上位中也一樣
  10 轉內沒過 → 結束，回到通常

通過 → 一轉定勝負（決定 BONUS 轉數）
  一般：無 10～20 轉／銅鐘・空掘 18～50 轉／紫機會牌 45～100 轉／**金機會牌 100～300 轉**
  上位：無 30～55 轉／銅鐘・空掘 45～80 轉／紫機會牌 70～100 轉／**金機會牌 150～300 轉**（保底 30 以上）

告知方式（玩家選）：
  強力鑽頭 → 直接告知轉數
  鏟子 → 點按次數隨轉數變化（大約 6～18 下），每下挖出 +N
      一下挖多少 = 單位 × 1／2／3／5（權重 50/28/14/8），單位 = 轉數 ÷ 20（最少 1）
      例：28 轉 → 單位 1 → 約 15 下，中間會夾雜 +3、+5
          176 轉 → 單位 8 → 約 12 下，會出現 +24、+40
      每個 +N 會散落在場景框的不同位置，不會重疊
  ※ 兩種一定導向同一個轉數（內部先決定，演出只是表現）

BONUS：純增，只賺錢（每轉挖到 1～3 個礦石，權重 30/40/30）
  BONUS 裡的機會牌只代表更多錢，不做任何抽選
  → 結束後回到 ST 挑戰（第 n+1 關）
```

## 4. 上位（取得三位前輩的認可）

- **抽選時機：通關第 1／3／7／10 關「之後」**（準備下一關時才抽、才演出），所以第一關絕對不會直接是上位。機率 15%／10%／60%／100%（設定6 為 22%／17%／72%／100%）。
- 每次進 AT 都會把關數與上位狀態重算，所以「上次打到 10 關」不會讓下一次的第一關直接上位。
- 上位之後：
  - 每次 BONUS 結束都可以**自己選前輩**挑戰（「A前輩，這次我要得到你的信任」）。
  - 壓對 → 通過率 90%；壓錯 → 70%。
  - 三種機會牌出現率變成 **1:1:1**（紫牌各 8%、金牌各 0.1%）。
  - 開了自動模式時，會**沿用玩家上一次手動選的前輩**（上位後一定得先手動選一次）。
  - ST 失敗才結束。

## 5. 收益（機械割的計算基礎）

礦石分成**三組，完全不重疊**（再乘礦坑倍率 2.2）：

| 階段 | 礦石 | 基本售價 |
|---|---|---|
| 通常 | 碎鐵砂／廢礦渣（普通礦）、前輩的舊釘（空掘） | 0.5 |
| 通常 | 工班的徽章（銅鐘） | 1 |
| 通常 | 岩倉的礦燈／赤井的鑿子／霧島的懷錶（紫機會牌） | 2.5／4／7.5 |
| 通常 | 岩倉的金牌／赤井的金鑿／霧島的金錶（金機會牌） | 6／9／15 |
| 報酬10轉・ST・一轉定勝負 | 工棚的鐵屑／工班的鑰匙 | 1.5 |
| 報酬10轉・ST・一轉定勝負 | 班長的筆記 | 3 |
| 報酬10轉・ST・一轉定勝負 | 岩倉／赤井／霧島的信任 | 6／8／12 |
| 報酬10轉・ST・一轉定勝負 | 岩倉／赤井／霧島的肯定（金機會牌） | 15／18／25 |
| **只在 BONUS** | 零錢袋／工資袋 | 3.85 |
| **只在 BONUS** | 加班費 | 7.7 |
| **只在 BONUS** | 岩倉／赤井／霧島的紅包 | 12.1／15.4／22 |
| **只在 BONUS** | 岩倉／赤井／霧島的獎金袋 | 30.25／38.5／52.25 |

**現金類的高價礦石（紅包、獎金袋、加班費、工資袋、零錢袋）只會在 BONUS 出現**，通常時、報酬 10 轉、ST、上位 ST 都不會挖到。已用實機測試驗證：各階段挖到的礦石名稱完全分開。

- v0.9.2 調整：礦石拆成三組（通常／報酬・ST／BONUS），高價現金類只留在 BONUS。BONUS 每轉的期望收入約是通常的 20 倍以上。
- 機械割 = (礦石基本售價 × 礦坑倍率) ÷ 工具花費，**只用上面的基本價**算，不含恩惠、委託 1.5 倍、廣告。
- 演出轉（約會對話、挑戰準備、選擇、鏟子點按）**不消耗耐久、不產生礦石**。

## 6. 實作與驗證

- 引擎：`js/engine2.js`（`newState2` / `step2` / `simulate2`），與 `engine.js` 完全獨立。
- 狀態：`normal / date / at / stIntro / st / reward / pick / dig / bonus`，存在 `save.plays2[礦坑id]`。
- 已驗證：
  - 40 萬次報酬抽選中，鏟子告知的轉數與內部決定的轉數 **100% 一致**（不會出現鑽頭 200 轉、鏟子 189 轉的情況）。
  - **自動模式在演出開始時會自動暫停**（談話開始、也想找你聊聊、上位、選擇前輩、選擇告知方式、一轉定勝負、ST 失敗）。玩家可以自己點著看，或再按一次自動＝快速跳過。
  - 離開礦坑後 `save.plays2` 該座的狀態整個刪除（累積、好感度全歸零）。
- 已驗證：ST 中抽到金機會牌 198 次，通關率 100%；鏟子挖出的總和與內部轉數 100% 一致。
- 已驗證：違和感長度（1～2 轉或 16～20 轉）**100% 都是會過的場合**；50 萬轉中出現率約 3.5%（佔會過場次的 12%）。
- 模擬（每設定 70 萬抽選轉，v0.9.3）：設定1 RTP 102.5%、初當 1/382、平均通關 3.28 關；設定6 RTP 138.9%、初當 1/356、平均通關 4.55 關。
- 自我檢測（200 萬轉）：第一關上位 0 次；上位抽選只出現在已通關 1／3／7／10 關；上位中的 BONUS 最少 30 轉；ST 金機會牌 244 次全部過關；鏟子總和 100% 一致。
- **v0.9.3 修正**：天井觸發的談話若失敗，之前會因為天井計數沒歸零而「每一轉都再觸發談話」（天井吃到飽）。已改成談話一開始就歸零。
- v0.9.4：上位 ST 的金機會牌改成 1:1:1；自動模式在上位沿用玩家上次手動選的前輩；新增今日心情、連續對手台詞、結算畫面。實測 26 個開發者按鈕全部有效。
- v0.9.3 清理：舊制度（累積第 n 個機會牌才被找、第 25 個保底）的 `call` 設定已刪除，現在只用好感度制。開發者按鈕「累積 +5」改成「好感度 +10%」。
- 為了平衡「金機會牌確定過關 + 100～300 轉」，AT／ST／BONUS／一轉定勝負裡的金機會牌出現率降為原本的 40%（通常時維持不變，因為那是好感度的來源）。

## 7. 離開礦坑

挖礦畫面右下角有「離開」按鈕，會先問：

> 真的要離開嗎
> 離開了礦坑之後，坑洞將會坍塌，搜尋的結果也將重置喔…

按「離開」→ 該礦坑的狀態（機會牌累積、好感度、本日數據、第一台的天井累積）整個刪除，回到地圖。按「留下」→ 什麼都不會變。

## 7b. 進入密碼（上鎖）

這座礦坑要輸入密碼才能進入，地圖上的「前往」會顯示 🔒。開鎖成功後**這台裝置**會記住，不用每次輸入。

- 密碼在編輯模式 →「前輩台」分頁可以改，也可以取消上鎖，或按「忘記這台裝置」讓它再問一次。
- 程式裡存的是密碼的雜湊（`config.locks.m6`），原本的數字不會出現在原始碼裡。
- **但這只是「防朋友順手偷看」等級的鎖**：遊戲是純前端，懂程式的人可以改瀏覽器的 localStorage 或直接讀程式繞過。真正要防，得等之後接帳號系統，由伺服器決定誰能進。

## 8. 每位前輩的台詞

`machine2.bossLines[前輩id]` 有六組，都可以放多句、隨機挑一句：`call`（找你談話）、`win`（約會成功）、`lose`（約會失敗）、`appear`（ST 出場）、`pass`（ST 通關）、`fail`（ST 失敗）。

`date`（約會對話）可以放**多套**，每套的行數就是約會的步數——抽到哪一套，約會就走那幾步。在編輯模式裡一行一套、用「｜」分隔每一步。

## 8b. 今日心情、連續對手、結算畫面（v0.9.4）

- **今日心情**：每天每座礦坑固定一位前輩心情好，該位的**約會通過率 +5%**。挖到他的機會牌時有 15% 機率出現暗示（「岩倉今天看起來心情不錯」）。設定與台詞在 `machine2.mood`。
- **連續對手**：ST 連續遇到同一位時改用專屬台詞（`bossLines[id].again`），例如岩倉的「又是你？」、赤井的「兄弟，我們真有緣。」
- **結算畫面**：一輪結束（ST 失敗）時跳出結算：通關關數、最大一次報酬轉數、本輪總收穫、是否進過上位、倒在誰手上。

## 9. 開發者測試介面（編輯模式 →「前輩台」分頁）

| 按鈕 | 作用 |
|---|---|
| 好感度加滿 | 指定前輩或三位全部設成 100%（下次約會必定成功） |
| 好感度 +10% | 指定前輩的好感度 +10%（＝被找去談話的機率） |
| 好感度直接設成 | 10%／50%／90% |
| 下一轉指定抽到 | 強制下一轉的小役（三張紫牌／三張金牌／銅鐘／空掘／碎石） |
| 約會（必成功／必失敗） | 直接跳進指定前輩的約會，結果先寫死 |
| 直接進報酬 10 轉 | 跳過前面，直接進 AT |
| 切換上位 | 直接開啟或關閉上位 |
| 直接進 BONUS | 指定 10／30／50／100／200 轉 |
| 重置這座礦坑 | 清掉累積與好感度 |

分頁上方會即時顯示目前狀態：state、三位的累積數、三位的好感度、對手、是否上位。這些在遊戲裡對玩家永遠不顯示。

## 10. 還沒做（下一批）

1. **凍結演出（等諾茲托特回家測試後決定）** — 詳見下面第 11 節。
2. 一次只能進一個礦坑的限制（你說先不用做）。
3. 上位的專屬畫面（目前只有標籤與金色邊框）。
4. 機械割設定差再拉大。

## 11. 待辦：凍結演出（規格待確認）

參考實機的「フリーズ」：拉霸之後畫面突然停住、不受操作、數秒後才動。**不是抽到才凍結，而是內部已經先決定了大獎才播這段**，所以凍結＝確定。實機發生率 1/8,192（輕）～1/400,000（重），《スマスロ北斗の拳》是 1/85,592，恩惠都是最高等級（繼續率 94% 的無想轉生、最上位 AT 確定等）。

我們的做法（不需要圖片或動畫檔，CSS＋計時器就夠）：

1. 點下去之後文字不出現、點擊提示消失、畫面暗下來。
2. 這段期間**點擊完全無效**（3～8 秒），自動模式強制停下。
3. 為了讓玩家知道不是當機：邊框極慢明滅、場景細微雜訊或光點慢慢浮現、`navigator.vibrate` 震動（iOS Safari 不支援）。保險機制：期間連點 5 次以上提早 1 秒結束。
4. 結束 → 畫面炸開，直接給恩惠。

**等確認的四題**（回答格式例如「1D 2A 3A 4A」）：

| # | 題目 | 選項 |
|---|---|---|
| Q1 | 在哪裡觸發 | A 通常時（確定最強）／B 連續演出・約會中（確定過關）／C 礦脈・BONUS 中（大量上乗せ）／D 全部 |
| Q2 | 多稀有 | A 約 1/20,000（推薦）／B 1/8,000／C 1/65,536 |
| Q3 | 凍結多久 | A 一般 4 秒、最高級 8 秒（越久恩惠越大，推薦）／B 固定 5 秒 |
| Q4 | 期間畫面 | A 全黑＋極慢明滅＋光點（推薦）／B 黑底一個字一個字跑／C 反轉白底黑字 |

## 12. v0.9.5 調整紀錄（M4-A）

- 三位前輩的機會牌機率**拉近**：原本霧島比岩倉難 5.6 倍，現在只差 3 倍（設定1）、2 倍（設定6）。
- 出現率標示「A → B」= 設定1 → 設定6；岩倉設定共通，**越難的前輩設定差越大**（霧島＝判別要素）。
- 上位抽選階梯放寬：第 1 關後 15%→24%、第 3 關後 10%→16%、第 7 關後 60%→95%（設定1→設定6），第 10 關後仍是 100%。
- 結果：機械割 設定1 132.1% / 設定2 139.1% / 設定3 148.8% / 設定4 164.7% / 設定5 168.4% / 設定6 182.1%（各 150 萬揮模擬）。
- 礦石價錢未更動，全部靠機率調整。


## 13. v0.10.0：外出演出

> 本節描述的機制 **[IMPLEMENTED]**（程式存在、本機模擬與瀏覽器測試通過），尚未實玩驗證。
> 數字說明用途，**正式值以 `js/config.js` 的 `machine2` 為準**。

### 13-1. 流程

```
挖到機會牌 → 好感度決定「會不會被找」
    ↓
約會開始：用該前輩的固定通過率抽 → 通過 / 失敗（結果在這裡就定案）
    ↓
抽「要用哪一套劇本演」：礦坑內談話 / 外出 / 激熱
    ↓
演出（不消耗耐久、不掉礦石）→ 揭曉
```

**這台沒有「書き換え」**：結果先定案，演出只負責表現。演出中不會改變結果。

### 13-2. 約會通過率（固定，好感度不影響）

| 前輩 | 設定1 | 設定6 | 機會牌出現率 |
|---|---|---|---|
| 岩倉 | 27% | 32% | 1/102（最好抽） |
| 赤井 | 46% | 52% | 1/169 → 1/135 |
| 霧島 | 76% | 82% | 1/308 → 1/205（最難抽） |

越難抽到牌的前輩，通過率越高——抽到霧島的牌本身就是好事。

### 13-3. 劇本抽選

| | 礦坑內 | 外出 | 激熱 |
|---|---|---|---|
| 通過時 | 62% | 30% | 8% |
| 失敗時 | 93.5% | 6.5% | 0.5% |

實測（6 萬場，設定1）：

| 劇本 | 出現率 | 看到它時的通過率 |
|---|---|---|
| 礦坑內 | 78.6% | 40.6% |
| 外出 | 17.2% | **79.3%** |
| 激熱 | 4.2% | **94.1%** |

外出**不是確定**，只是高信賴度。

### 13-4. 外出地點

| 前輩 | 外出 | 激熱 |
|---|---|---|
| 岩倉 | **森林**：介紹小動物、講植物特性，說出自己本來想當植物學家 | **街上**：買衣服，你看到她少女的一面 |
| 赤井 | **酒館**：划酒拳、大聲吹牛 | **他家**：一堆弟弟妹妹，他一個人扛起整個家 |
| 霧島 | **封坑口**：訴說礦道的可怕、當年死掉的十七個人 | **礦山神社**：小時候想當忍者，家人離世後十一歲就下坑。**他笑出聲** |

每套四組台詞：`depart` 出發／`during` 現場／`win` 成功／`lose` 失敗，全部可在編輯模式改。

### 13-5. 期待度顏色

循序漸進、**只升不降**。大約演到 35% 之後開始升色，最後一轉是這場的最高點。

- **失敗時「紅」的權重是 0 → 紅色出現＝確定過關**
- 外出但會失敗 → 最多升到綠，玩家可以感覺到這次要失敗了
- 激熱一開場就是綠（`colorFloor.hot = 3`）

### 13-6. 違和感

保留，但**不再暗示**。以前標籤會寫「談話中（怪異）」直接洩漏答案，v0.10.0 拿掉了。

長度改成：短 **1~2 轉**、長 **20~30 轉**（舊的 16~20 太接近一般長度，玩家看不出來）。

### 13-7. 連續被拒彩蛋

約會失敗會記住是誰（`st.lastRejected`）。下一次又是同一個人 → 用 `again` 台詞取代開場白。
**v0.9.x 的 `again` 用錯位置**（接在 ST 連續遇到同一人），v0.10.0 已改正。

### 13-8. 鏟子挖轉數（改）

舊做法是把總轉數平均切開，抽到 214 轉就一直跳 +25 +50，很無聊。
新做法：大部分是 **+1／+2／+3／+5**，只有小數字補不完時才夾帶幾個**大跳**（+25、+50、+100…）當驚喜。前 3 下不會出現大跳。總和一定等於實際轉數（20 萬次測試 0 次不符）。

實例：

```
 34 轉 → 11 下：5 1 3 3 10 2 1 1 5 2 1
 89 轉 → 17 下：2 3 5 5 3 1 1 5 1 2 50 1 1 1 2 5 1
219 轉 → 30 下：2 2 2 5 100 1 1 3 5 1 1 5 2 1 5 2 1 50 3 3 3 3 3 3 2 2 1 1 2 4
```

### 13-9. 修正的 BUG

- **上位後關卡數會多跳**：`st.stRound++` 寫在「要玩家選前輩」的提早 return 之前，所以每次選前輩都會多加一次。已移到 return 之後。實測：選前輩前後都是第 1 關。
- **死欄位 `date.favorMin / favorMax`**：沒有任何程式讀它們，已刪除。
